#### common
export PROFILE=""
export VPCID=""
# SimPrivateSubnet1_ELB_EC2, SimPrivateSubnet2_ELB_EC2
export SUBNETIDS=""
export REGION=""

### Lambda
export LAMBDA_STACK_NAME=""
export TARGET_LAMBDA_ARN=""
export LAMBDA_TEMPLATE_PATH=""
export ROUTETABLEIDS=""
export LAMBDA_ROLE_ARN=""
# logonlogoff_apigw_lambda.yaml 用（VPCE を新規作成するか/既存を使うか）
# - 新規作成する場合: EXECUTE_API_VPCE_CREATE=""
# - 既存を使う場合:   EXECUTE_API_VPCE_CREATE="" とし、EXECUTE_API_VPCE_ID に vpce-xxxx を設定
export EXECUTE_API_VPCE_CREATE=""
export EXECUTE_API_VPCE_ID=""
# lambda_deploy.sh 用（S3/SSM VPCE を新規作成するか/既存を使うか）
# - S3 Gateway は既存を活用（スクリプトが自動検出。見つからない場合は S3_VPCE_ID を指定）
# - SSM Interface は「存在すれば作成しない / なければ作成する」を自動判定
export CREATE_S3_ENDPOINT=""
export S3_VPCE_ID=""
# CREATE_SSM_ENDPOINT は未設定(空)なら自動判定。強制したい場合のみ true/false/false2 を設定。
# falseの場合は、すでに SSM VPCE が存在することを前提とする。
# ただし false2 を指定した場合は、存在しなくてもエラーとはしない（SSM VPCE がなくても動作する Lambda の場合など）。
export CREATE_SSM_ENDPOINT=""
export SSM_VPCE_ID=""
export BUCKET_NAME=""
export LAMBDA_SECURITY_GROUP=""
export VPCE_SECURITY_GROUP=""

### API Gateway
export STACK_NAME=""
export API_TEMPLATE_PATH=""
export IPSEGMENT=""
# Client VPN 経由の到達を許可する場合は、Client VPN の Target Network Association に付与されている SG を指定
#export CLIENT_VPN_SG_ID=""
# apigw_deploy.sh 用（Lambda permission）
export STATEMENT_ID=""
export FUNCTIONNAME=""
# API Gateway execute-api VPCエンドポイントにアタッチするセキュリティグループを指定
export EXECUTE_API_ENDPOINT_SG=""
# API Gateway の CloudWatch Logs へのアクセスに使用する IAM Role ARN を指定
export APIGW_CLOUDWATCH_LOGS_ROLE_ARN=""
